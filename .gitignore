# Python
__pycache__/
*.pyc
*.pyo

# uv / venv
.venv/

# Disparity outputs (regenerated by main.py)
/Tsukuba.png
/Venus.png
/Teddy.png
/Cones.png

# Bonus pipeline outputs and downloads (not artifacts to track)
/data/
/out/

# Bundle build output (produced by scripts/package_bonus.sh)
/dist/

# Vendored third-party source pulled at build time (large + has its own LICENSE)
/third_party/

# Local Claude Code session state (per-machine, not source)
.claude/

# Old / one-off archives at the repo root (not source)
/Stereo_Matching.zip
/*_final.zip

# LaTeX build artifacts (keep .tex / .bib / .pdf, drop intermediates)
*.aux
*.bbl
*.bcf
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.run.xml
*.synctex.gz
*.xdv
*.toc
*.nav
*.snm
*.vrb

# OS / editor
.DS_Store
.vscode/
.idea/
