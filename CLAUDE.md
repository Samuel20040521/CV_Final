# CLAUDE.md — NTU CV 2026 Stereo Matching Final Project

操作這個 repo 時必須遵守的硬性限制與背景。資料來源：
- `doc/final_project_stereo_matching_2026/final_project_stereo_matching_2026.md`（主題目 doc）
- `doc/CV_Final_Project_Rules/CV_Final_Project_Rules.md`（助教補充規範，**Stereo Matching Team** 段落）

兩份來源有衝突時，以**補充規範**為準（後出文件、更具體）。

## 基本題的硬性限制

### 可以改 / 不可以改的檔案

| 檔案 | 狀態 | 規則 |
|---|---|---|
| `computeDisp.py` | TODO（學生實作） | **唯一需要繳交的檔案**（見 §Submission） |
| `eval.py` | 助教評分用 | **DO NOT EDIT this file** — doc 原文明確規定 |
| `main.py` | 助教已完成 | doc 標 "completed"；助教指定的 CLI：`python3 main.py --image Teddy`，不可改介面 |
| `requirement.txt` | 環境檔 | 助教指定 "可安裝套件如助教提供的 requirement.txt"，必須保留可用 |

延伸到本專案的實作選擇：
- `computeDisp.py` 已改為 1 行 shim（`from stereo_matching import computeDisp`），把實作搬進 `stereo_matching/` 套件。**對助教的角度看，公開介面仍是 top-level 的 `computeDisp.computeDisp(Il, Ir, max_disp)`，不能變。**
- 任何重構都不可破壞 `from computeDisp import computeDisp` 的 import 契約（`eval.py:6` 和 `main.py:6` 都依賴它）。

### 環境

- **Python 3.8**：補充規範原文「作業環境請以 python3.8 運作」。主 doc 寫 3.6+ 是寬鬆下限；以 **3.8 為硬性目標**。
  - 本專案 `.python-version` 目前 pin 在 3.11 — 用於本地開發 OK，**但繳交前要驗證 code 在 3.8 跑得起來**（避免 PEP 604 union、PEP 612 ParamSpec、match 等 3.10+ 語法；型別註解上的 `int | None` 改成 `Optional[int]`）。
- **套件**: `pip3 install -r requirement.txt`。本 repo `requirement.txt` 內容：`numpy / opencv-contrib-python / pandas / tqdm / psutil`。
  - `opencv-contrib-python`（不是 `opencv-python`）— `cv2.ximgproc` 在 contrib 才有。
- **指定 CLI**（補充規範原文）：
  - `python3 main.py --image Teddy`
  - `python3 eval.py --image Teddy`
  - 助教會按這兩條命令跑，不可變動。

### 演算法限制

- ✅ **可以**使用 `cv2.ximgproc`（doc 明示 "Good News: you CAN use cv2.ximgproc"）。本專案用到 `createGuidedFilter` 與 `weightedMedianFilter`。
- ❌ **不可以**使用 deep learning。doc 原文 "CANNOT use deep learning based methods."
- 每張圖會給定 `max_disp`，不必搜超過此範圍（`eval.py` 的 config：Tsukuba=15, Venus=20, Teddy=60, Cones=60）。

## 評估架構（補充規範 §一.2）

| 場域 | 圖數 | 說明 |
|---|---|---|
| 本機 | 1 組 | 我們手上有 `testdata/Teddy/`。可用 `python3 eval.py --image Teddy` 直接驗 BPR。 |
| Codalab | 3 組（含本機那組） | 助教跑線上評測；Codalab URL 待公布 |
| 助教 hidden | 1 組 | 6/2 發布，助教用我們 6/1 繳交的程式碼在助教電腦上跑 |
| **最終評分** | **4 組** | **Codalab 3 組 + hidden 1 組** |

→ 換句話說：本機只能跑 1 張圖，其餘必須上 Codalab；hidden 那張完全看不到，要靠演算法夠通用。
→ **不能 overfit 到 Teddy**。任何 hyperparameter 都要假設會被搬到陌生圖。

## 評分結構（Total 105%）

### Performance: 80%

四組圖各自的 bad pixel ratio（error threshold = 1）必須過門檻：

| Dataset | BPR 門檻 | `eval.py` scale_factor | `max_disp` |
|---|---|---|---|
| Tsukuba | < 8% | 16 | 15 |
| Venus | < 5% | 8 | 20 |
| Teddy | < 18% | 4 | 60 |
| Cones | < 15% | 4 | 60 |

（這 4 個是主 doc 列的範例門檻；實際 Codalab 上的 3 張 + hidden 是哪幾張要等公布。）

### Ranking

主 doc 公式被 PDF→Markdown 轉換截斷（`Ranking: (ς=1 4)*`），按 4 張圖綜合排名給分。

### Bonus: 25%

真實場景 pipeline + demo 影片，**與基本題分離繳交**（補充規範 §二.4，跟報告一起 6/5 前寄）。本專案 `bonus/` 目錄處理這部分。

### 罰則（嚴重）

- **−30 分**：任一張圖處理 > 10 分鐘。
- **−20 分**：任一張圖未過 BPR 門檻。

→ 兩個都是按「任一張」觸發，所以 4 張都要先安全過門檻才談優化。**hidden 那張的安全邊際**特別重要——調 hyperparameter 不要把任何已知圖逼到剛好過門檻。

## 繳交格式與時程（補充規範 §二，跟主 doc 衝突時用這個）

### 6/1 23:59 — 最終程式碼

- 寄到 **<fushengyu@media.ee.ntu.edu.tw>**（email，不只是 NTU COOL）
- 內容：
  1. python code（含 `computeDisp.py` 與其依賴）
  2. 一個 `.txt` 文字檔，**放目前跑出的數據**
- **遲交以零分計算**
- 6/1 之後 code 不能再改 — 6/2 助教用這份 code 跑 hidden image

### 6/2 — 助教跑 hidden image

- 助教用我們 6/1 繳交的程式碼在助教電腦跑 hidden 那組
- 公布所有組別的成績結果（評分基準之一）

### 6/5 23:59 — 報告與加分題

- 寄到同信箱：
  1. 書面報告（PDF）
  2. 投影片報告
  3. 加分題的程式碼
  4. 一個 `.txt` 文字檔，講解**加分題程式**怎麼跑（檔案放置、command line、目前跑出的數據）
- 報告與投影片需要：
  - 把 6/2 助教幫你跑的模擬結果放**前幾頁**
  - 解釋加分題內容（可拍影片展示結果）
- **遲交以零分計算**

### 繳交目錄結構（主 doc §Submission）

- `StudentID_final/computeDisp.py`（目錄名固定，明列只有這檔）
- Zip 成 `StudentID_final.zip`
- 因為本專案把實作模組化到 `stereo_matching/`，**打包時必須把整個 `stereo_matching/` 套件一起放進 `StudentID_final/`**，不能只放 shim。
- 另外加上 `requirement.txt`（助教要 pip install）。

## 公開排行榜

補充規範 §一.3 只在 Frame Generation Team 段落提到加分排行榜（5/18 - 5/31，前三組加分）。**Stereo Matching Team 段落沒提排行榜抽選加分機制**，所以這項不適用我們。

## 目前狀態快照

- `main` branch：基本題 pipeline 完成，commit `60e369f Initial stereo matching pipeline — Census + guided filter + WMF`
- `Teng` branch：bonus 相關 commit
- **本機驗證過的成績**：Teddy 11.85% BPR（門檻 18%）、1.07s（門檻 600s）
- **未驗證**：Tsukuba / Venus / Cones — 本機沒資料，要靠 Codalab 才能跑

## 操作這個 repo 時對 Claude 的提醒

1. **不要動 `eval.py`**。任何修改都會違反 doc 規則。
2. **保留 top-level `computeDisp.py` 的 import 介面**。改它的內部 OK，但 `from computeDisp import computeDisp` 必須仍然 work。
3. **`main.py` 視為唯讀**，除非使用者明確要求改，且 CLI `python3 main.py --image Teddy` 不能變。
4. **目標 Python 3.8**：新 code 避免 3.10+ 才有的語法（PEP 604 union、match-case、PEP 612）。型別註解上的 `int | None` 寫成 `Optional[int]`，`list[int]` 寫成 `List[int]`（並 `from typing import ...`）。
5. **不要 overfit 到 Teddy**。本機唯一能驗的圖是 Teddy，但 final 評分包含 hidden 圖；hyperparameter 不要逼到任何單一圖剛好過門檻。
6. **評估改動前先想罰則**：是否會讓某張圖跨過 10 分鐘？是否會讓 BPR 退步到門檻邊緣？任何效能優化都要回跑 `uv run python eval.py --image Teddy` 驗證。
7. **時程意識**：6/1 程式碼凍結（email 寄出），6/5 報告與加分題寄出。6/1 之後 code 不能再改。
